console.log(document.body);

window.onload = function() {
    console.log(this.document, body);
    init()
};

const init = () => {
        Cconsole.log(document.body)
        const request = await fetch("/all-posts");
        const request = fetvh(“/all-posts”);
            console.log(request);
        }